# Audit T140.5B — LoopService + LoopMessageService → organization_id-first

## Résumé

Migration des 9 `community_id` références dans LoopService (8) et LoopMessageService (1) vers `organization_id`-first, avec fallback `community_id` conservé. Orchestration multi-agents : TECH_WRITER → TEST_WORKER → STEP_GLOBAL_REVIEWER → REVIEW_SUPERVISOR.

## Changements

### Runtime — LoopService.php (7 changements logiques)

| Méthode | Avant | Après |
|---------|-------|-------|
| `createLoop()` | `$communityId = $user->community_id` | `$orgId = $user->organization_id ?? $user->community_id` |
| `createLoop()` | `'community_id' => $communityId` | `'organization_id' => $orgId` |
| `addMember()` | `$loop->community_id !== $user->community_id` | `$loop->organization_id !== $orgId` |
| `getEligibleReferrals()` | Guard + 2x `where('community_id', ...)` | `organization_id` partout |
| `addReferralToLoop()` | `$referral->community_id !== $loop->community_id` | `$referral->organization_id !== $loop->organization_id` |
| `generateUniqueSlug()` | `where('community_id', $communityId)` | `where('organization_id', $orgId)` |

### Runtime — LoopMessageService.php (1 changement)

| Méthode | Avant | Après |
|---------|-------|-------|
| `assertCanSend()` | `$loop->community_id !== $sender->community_id` | `$loop->organization_id !== $orgId` |

### Tests (6 assertions de message corrigées)

- LoopCreationTest.php:69
- LoopMessageTest.php:129
- LoopMemberInvariantTest.php:67, 99, 202

### Agents

| Agent | Tâche | Verdict |
|-------|-------|---------|
| TECH_WRITER | Implémentation code | ✅ COMPLETE |
| TEST_WORKER | Test suite + fix assertions | ✅ 221/221 passed |
| STEP_GLOBAL_REVIEWER | Revue périmètre + logique | ✅ GO |
| REVIEW_SUPERVISOR | Verdict final | ✅ GO |

## Compatibilité

- `HasOrganizationId` sur Loop/Referral synchro `organization_id` avec `community_id`
- Fallback `$user->community_id` si `$user->organization_id` est null
- Aucune migration DB

## Risques

- `HasOrganizationId` auto-sync peut masquer d'éventuels bugs (les deux colonnes égales en test)
- LoopController (T140.5D) utilise encore `$loop->community_id` — incohérence temporaire, scope séparé

## Rollback

```bash
git revert <commit-hash>
```
