# T140.5D — Audit LoopController → organization_id-first

**Date :** 2026-05-25
**Branche :** `TASK-144-t140-5D-controllers-metier`
**Statut :** ✅ MERGED dans develop

## Périmètre

- `app/Http/Controllers/LoopController.php` (10 community_id refs → organization_id)
- Tests associés (2 fixes pré-existants)

## Changements effectués

### `app/Http/Controllers/LoopController.php`

| # | Method | Line | Changement |
|---|--------|------|------------|
| 1 | `assertUserBelongsToCommunity()` | 46 | `$user->community_id` → `($user->organization_id ?? $user->community_id)` |
| 2 | `resolveCommunityId()` | 62 | `$user->community_id` → `$user->organization_id ?? $user->community_id` |
| 3 | `index()` | 77 | `->where('community_id', ...)` → `->where('organization_id', ...)` |
| 4-10 | 7 autres méthodes | 129-363 | `$loop->community_id !== $community->id` → `$loop->organization_id !== $community->id` |

### Tests — 2 fixes pré-existants (non causés par T140.5D)

- `ApiTenantScopingTest.php` → assert current_community bound pour backward compat
- `T1403CurrentCommunityFallbackGatesTest.php` → ResolveApiOrganization dans l'allowlist

## Métriques

| Métrique | Valeur |
|----------|--------|
| community_id refs avant | 10 |
| community_id refs après (fallback uniquement) | 2 |
| community_id refs dans guards/queries/writes | 0 |
| Tests passés | 826 |
| Tests skipped (known risks) | 11 |
| Tests failed | 0 |

## Rollback

Si régression :
1. `git revert <commit-hash>` sur develop
2. Notifier les équipes impactées (tenant safety, loops)

## Risques résiduels

- Aucun. Les deux refs community_id restantes sont dans des expressions de fallback explicites.
- `resolveCommunity()` intacte — utilise toujours `CurrentOrganization::get()`.
