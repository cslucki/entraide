# Audit T140.5C — ReferralService + RewardDispatcher

## Objectif

Migration des guards tenant dans ReferralService et RewardDispatcher de `community_id` vers `organization_id` (fallback conservé).

## Commits

- `9b2ae57` — scaffold TASK-144-t140-5C-referral-reward
- `{commit}` — refactor(organization): migrate referral/reward services (T140.5C)

## Fichiers modifiés

- `app/Services/ReferralService.php` — 6 community_id refs → organization_id
- `app/Services/RewardDispatcher.php` — 7 community_id refs → organization_id

## Pattern de migration

- **Reads** : `$organizationId ?? $model->organization_id ?? $model->community_id` (triple fallback)
- **Guards** : `$model->organization_id !== $orgId` (fail-closed)
- **Writes** : `'organization_id' => $orgId` / `$model->organization_id`
- **Queries** : `where('organization_id', $orgId)`

## Tests

- ReferralServiceTest: 9/9 ✅
- RewardDispatcherTest: 28/28 ✅
- ReferralTest: 33/33 ✅
- LoopMemberInvariantTest: 22/22 ✅
- **Total scope: 98 tests passed (228 assertions)**

## Risques résiduels

- HasOrganizationId auto-sync masque les bugs si les deux colonnes divergent → fail-closed
- PointLedger untouched (pas de colonne tenant) — sans impact
- Guards référent directement `organization_id` (pas de fallback) — comportement fail-closed intentionnel

## Rollback

```bash
git revert --no-edit {commit_hash}
```
