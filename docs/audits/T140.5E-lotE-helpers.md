# T140.5E Lot E — helpers.php organizationRoute param rename

**Date :** 2026-05-25
**Branche :** `TASK-144-t140-5E-lotE-helpers`

## Changement unique

`app/Support/helpers.php:30-33` — Inversion du mapping de paramètres dans `organizationRoute()`.

## Avant

```php
if (isset($parameters['organization']) && ! isset($parameters['community'])) {
    $parameters['community'] = $parameters['organization'];
    unset($parameters['organization']);
}
```

## Après

```php
if (isset($parameters['community']) && ! isset($parameters['organization'])) {
    $parameters['organization'] = $parameters['community'];
    unset($parameters['community']);
}
```

## Explication

Les routes migrent de `{community}` vers `{organization}`. La fonction `organizationRoute()` normalise le paramètre pour `route()`. Avant, elle mappait `organization` → `community` (legacy). Maintenant elle mappe `community` → `organization` (futur).

## Impact

- **Call sites :** 0 — `organizationRoute()` n'est utilisé nulle part actuellement
- **Tests :** 826 pass, 0 fail, 11 skipped
- **Risque :** NULL

## Rollback

```bash
git revert HEAD
```
